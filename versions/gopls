#!/bin/bash
set -o errexit
set -o nounset
set -o pipefail

CHANNEL="${1:-stable}"
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "${SCRIPT_DIR}/lib"

# gopls ships from the golang/tools repo with tags like gopls/v0.21.1.
TAG="$(github_latest_tag golang/tools)"
echo "${TAG#gopls/v}"
